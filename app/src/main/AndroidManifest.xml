<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.student.SupportActivity"
            android:exported="false" />
        <activity
            android:name=".ui.student.ForumActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.ConfirmCodeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.ResetPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.student.LessonActivity"
            android:exported="false" />
        <activity
            android:name=".ui.student.CourseDetailActivity"
            android:exported="false" />
        <activity
            android:name=".ui.student.StudentActivity"
            android:exported="false" />
    </application>

</manifest>